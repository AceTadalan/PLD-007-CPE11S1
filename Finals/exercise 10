#include <cstdlib>
#include <algorithm>
#include <ctime>
using namespace std;

int main () {

	/*string str = "Hello, World!";
cout << "Length of string: " << str.length() << endl;
cout << "Substring: " << str.substr (7,5) << endl;

return 0;
}
